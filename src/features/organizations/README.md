# Organizations (deferred)

Not built yet.

**Planned:** Client organizations (the nonprofits, public-health agencies, and
civic bodies that issue RFPs) as first-class records. Each will have contacts,
history, and links to its past RFPs. Today the `client` field on an RFP is plain
text.

See `docs/data-model.md` → _Organization_.
